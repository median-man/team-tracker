module.exports = {
  createNote: (req, res) => {
    res.json({ message: "TODO: create note" });
  },
  deleteNote: (req, res) => {
    res.json({ message: `TODO: delete note ${req.params.id}` });
  },
};
